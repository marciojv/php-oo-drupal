<?php

function themeview_preprocess_views_view(&$variables) {
  $view = $variables['view'];

  if ($view->id() == "escola") {
    $variables['title'] = 'Subtitulo da Pagina View';

    // Imprima todas as chaves para ver o que você tem disponível
    // print_r(array_keys($variables));
  }

}
